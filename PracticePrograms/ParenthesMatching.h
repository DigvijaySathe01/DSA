#pragma once

// Checks if the parentheses in the given expression are balanced.
bool ParenthesisMatching(char* expression);

// Checks if the given expression contains balanced parentheses, brackets, and braces.
bool MultiParenthesisMatching(char* expression);

//Function to convert infix expression to postfix expression
char* InfixToPostFix();